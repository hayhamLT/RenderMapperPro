# Python
__pycache__/
*.py[cod]
*.pyo

# Virtual environments
.venv/
.build-venv-py3.12/
venv/
env/

# Build / distribution
build/
dist/
*.egg-info/
installer/Output/      # Inno Setup compiler output (Setup.exe), produced in CI
*.dmg                  # locally-built disk images (CI uploads its own)

# macOS
.DS_Store

# PyInstaller
*.spec.bak

# Vendored static ffmpeg/ffprobe binaries (fetched by build_standalone.sh)
vendor/ffmpeg/

# Auto-update token baked in by CI — never commit
assets/update_token.txt
